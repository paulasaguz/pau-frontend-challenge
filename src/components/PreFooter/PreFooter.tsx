import styles from './PreFooter.module.css';

const PreFooter = () => {
  return (
    <div className={`${styles.root} bg-center w-full lg:bg-contain bg-cover bg-repeat-x h-12 m-0`}></div>
  );
};

export default PreFooter;
